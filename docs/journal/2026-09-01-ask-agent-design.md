# sumac: Local In-Process Agent for `sumac ask`

**Status:** design record, drawn from an external ideation session; implementation not started
**Author:** drafted with Claude, 2026-09-01
**Scope:** new `sumac/llm.py` (or equivalent), `cli.py`'s `ask` command, no changes to `decide.py`/`ledger.py`/`events.py` domain logic

---

## 1. Motivation

The domain model (event log, ledger fold, `decide` gate) is already correct and already tested against a real household's usage. The friction is entirely at the interface: a single physical action — "take a ragu out of the freezer and a tin of tomatoes out of the pantry, put ragu-for-one and a jam jar of tomatoes in the fridge, second shelf" — decomposes into several `sumac add` invocations, each requiring information the user must look up by hand first: the exact product string as stored (`"Homemade Ragu"` vs `"Home Made Ragu"`, two distinct products), the exact location id (`fridge-main-shelf-2`, not "second shelf"), and the exact unit already on record (`bags`, not `bag` — `decide_change` rejects the singular with `unit_unconvertible` since unit conversion is nominal per `Config.convert`, not a pluralisation rule). A worked session doing exactly this — recorded in full below in §2 — took thirteen commands and three dead-end attempts to complete a task a person would describe in one sentence.

The session's own conclusion, reached before any code existed, is the thesis this entry inherits: the CLI's command surface is a fine *domain API* and a poor *human interface* for compound, underspecified requests. The fix is not to make the CLI's argument parsing smarter — `sumac add`'s job is to be an unambiguous, scriptable operation, and it should stay that way — but to add a second, narrower entry point that accepts an underspecified sentence, resolves it against the real inventory, and asks before writing anything.

## 2. How the idea developed

The starting point was a real interactive session (transcript preserved in this repo's chat history, not reproduced verbatim here) in which the user asked an external LLM, command by command, how to record that a jar of jam was eaten, then how to execute the ragu/tomatoes compound request above. That session surfaced two facts that shaped everything after it:

- **The chore was real and specific, not a vague UX complaint.** `sumac find` already returns exactly the disambiguating information a human needs (product name, quantity, unit, full location path) — the burden was manually transcribing that output back into `sumac add`'s positional arguments, correctly, every time.
- **The compound request was not a `movement`.** The ragu was cooked from frozen base ragu and repackaged into a Tupperware box under a new name ("ragu for one"); the tomatoes were decanted from a half-used tin into a jam jar. Both product identity *and* unit changed. The session's own resolution was two `consumption` events (against the original products, from their original locations) followed by two `discovery` events (the new products, into the fridge) — four writes, not one `movement` with a relabelled endpoint. This is the shape any agent built for this domain has to reproduce: **one user utterance can require an ordered sequence of distinct domain operations**, not a single tool call.

From there the design question moved through several rejected shapes, each rejected for a stated reason rather than by default:

- **A rewritten, "smarter" `sumac add`** (inferring source location from a unique `find` match, accepting plurals, taking a free-text destination like "fridge second shelf") — rejected as the first idea, because it still requires the human to decompose a compound request into separate CLI invocations; it improves each command without addressing the ragu/tomatoes case at all.
- **A general chat/GUI product** (conversational history, a `sumac` REPL with a chat transcript, eventually a web UI with a command palette) — the broader idea explored the shape of this at length, but it was set aside as out of scope for a first slice: it multiplies the surface area (rendering, session state, undo) before the core translation — sentence to domain calls — is shown to work at all.
- **Ollama** — rejected as a dependency: it runs as a separate local server process, which conflicts directly with the constraint that `sumac ask` is a plain CLI invocation with no daemon and no network port, local or otherwise.
- **`llama-cpp-python`** — considered viable (in-process, CPU-capable, GGUF, has tool-calling support) and not rejected, but superseded once mistral.rs's Python SDK was found to offer the same shape with a tool-callback API already built around exactly this pattern.
- **mistral.rs's `mistralrs serve --agent`** — this was the one genuine near-miss, because the documentation page found for it (`docs/agents/build-an-agent`) is titled "agents" and looked at first glance like the intended entry point. It is not: that mode runs a server process (`mistralrs serve`) and hands the model a fixed, generalist toolset — web search, arbitrary Python execution, shell execution — none of which sumac's domain permits an LLM to touch. Distinguishing this from the Python SDK's *in-process* agentic callback API (`examples/python/agentic_tools.py`, quoted in full in the originating request) is the pivot the design settled on.
- **LangChain, CrewAI, MCP, or any other agent-orchestration framework** — rejected throughout on the same grounds: the tool surface is fixed at roughly five typed Python functions, and the loop that dispatches a tool call and feeds back its result is exactly what mistral.rs's `Runner.tool_callbacks` + `max_tool_rounds` already does, in-process, with no added framework.

## 3. What this is

`sumac ask "<sentence>"` is a CLI command, alongside the existing ones, that:

- Loads a small GGUF instruct model (roughly 0.6B–1.7B parameters, chosen for CPU inference speed over raw capability — the model's job is narrow semantic parsing against a five-tool vocabulary, not general reasoning) via mistral.rs's Python `Runner`, in-process, inside the same Python interpreter running the CLI. No server, no port, no daemon left running after the command exits.
- Registers a small, fixed set of Python tool callbacks on that `Runner` — not the CLI's own commands, but thin wrappers around the domain layer's existing entry points (`ledger.build_inventory` for search/lookup, `decide.decide_change` for consumption/movement/discovery, `decide.decide_correct` for corrections). Registered via `Runner(..., tool_callbacks={...})` and driven with `max_tool_rounds` set on the `ChatCompletionRequest`, per the SDK example that motivated this design — the engine executes the loop of "model proposes a tool call → sumac's Python code runs it → the result is fed back to the model" without any additional orchestration code.
- Can and is expected to produce **more than one domain call per invocation** — the ragu/tomatoes case (§2) is the reference example: one sentence, four writes (two `consumption`, two `discovery`), each going through the same `decide_change` gate `sumac add` uses today. The agent's role is to plan and sequence those calls, not to reimplement or bypass what `decide_change` already validates.
- Searches before acting: given "take a ragu out of the freezer," the tool wrapping inventory lookup is called first, and its actual result (the real matching products, quantities, units, and locations — not anything the model asserts on its own) is what the model reasons over for the next tool call. This mirrors `sumac find`'s existing output exactly; the agent is not given a dump of the whole inventory, only what a query returns.
- Asks the user to disambiguate rather than guessing, when a search returns more than one plausible match with no basis in the sentence for picking one — the stated example being `find_inventory("ragu")` returning both "Home Made Ragu" (1 bag) and "Homemade Ragu" (7 bags): the agent presents both and stops rather than choosing.
- Surfaces the same domain errors `sumac add` would produce — `unit_unconvertible`, `unknown_location`, `missing_endpoint`, and the rest of the rejection catalogue in `docs/journal/2026-08-30_decide-pattern-data-integrity-upgrade.md` §4 — back to the model as a tool result, not as a crash, so the model can retry with a corrected call or explain the failure to the user rather than the process exiting on an unhandled exception.

## 4. What this is not

- **Not a replacement for the existing CLI.** `sumac add`, `sumac find`, `sumac status`, and every other command are unchanged and remain the deterministic, scriptable path — the point of `mv`-style commands (fully specified by their arguments, no interpretation required) is precisely that they do not need a confirmation step, and nothing about adding `ask` changes that property for the rest of the CLI.
- **Not the mistral.rs generalist agent mode.** `mistralrs serve --agent`'s built-in web-search, shell, and Python-execution tools are explicitly not used. The LLM's only capabilities are the specific typed functions sumac registers; it has no path to the filesystem, the database, or arbitrary code, satisfying the constraint that the domain layer stays authoritative and the model can only invoke what is explicitly exposed to it.
- **Not a chat product.** The interaction is one request in, one proposed action (or one clarifying question) out, then either confirmation or cancellation — not a persisted multi-turn conversation with history that itself becomes a feature. `sumac`'s existing model deliberately does not track chat history between calls (matching mistral.rs's own note that its `Runner` does not track chat history across separate `send_chat_completion_request` calls) — each `sumac ask` invocation is independent.
- **Not a source of new domain rules.** Every operation the model can trigger already exists in `decide.py`. Nothing about this design adds a new `ChangeKind`, a new event type, or a new validation path — the "discovery" and "consumption" events used in the ragu/tomatoes example (§2) are the existing `discovery` and `consumption` kinds `sumac add` already accepts, invoked in sequence rather than added to.
- **Not restricted to interactive human use.** `sumac ask` is a CLI command like any other and can be driven by a script or by another agent, not only typed at a terminal by a person. The propose-then-confirm behaviour described in §5 belongs to the interactive path; a non-interactive caller (a script, a cron job, another agent) is a different consumer of the same underlying "sentence in, proposed writes out" mechanism and owns its own decision about whether and how to gate execution — this design does not assume a human is always the one pressing confirm.

## 5. Why a confirm step, and why only here

Every other sumac command is unambiguous by construction: `sumac add consumption "Strawberry Jam" 1 jar --from fridge-door` fully determines what will be written before it runs, the same way `mv a b` fully determines the filesystem operation before it runs — there is nothing to interpret, so there is nothing to confirm. `sumac ask` is different in kind, not degree: the input is a sentence that requires interpretation — resolving "a ragu" against two candidate products, resolving "second shelf" against a location id, deciding that "a jam jar full of tomatoes" describes a new product-and-unit pair rather than a straight move — before it can become a determinate operation at all.

That interpretation step is exactly where a small model can be wrong in a way `sumac add`'s argument parser cannot: parsing `"1"` as an amount has one meaning, but resolving "the ragu" against inventory has several. The proposal is therefore: `sumac ask` resolves the sentence into a concrete, fully-specified sequence of the same writes `sumac add` would produce, shows that sequence to the user before any of it is written (mirroring the search-first, ask-when-ambiguous behaviour in §3), and only calls into `decide_change`/`store.append` after the user accepts it. This keeps the domain layer's guarantees intact — every write, agent-proposed or hand-typed, passes through the identical `decide_change` gate — while adding a review step at the one point in the system where the input itself is ambiguous rather than the operation.

## 6. What is not yet decided

- The exact tool function signatures (parameter shapes for search/consume/move/discover) and how closely they mirror `decide.decide_change`'s existing keyword-argument shape versus a narrower agent-facing wrapper.
- How the proposed write sequence is rendered to the user for confirmation — plain text, a rich table via `render.py`, or output shared with `sumac doctor`'s existing anomaly-table rendering.
- Which specific GGUF model and quantisation is used by default, and where the model file is expected to live on disk (a cache directory under `paths.py`'s existing layout, or left to mistral.rs's own Hugging Face cache resolution).
- Whether `sumac` with no arguments becomes an interactive agent REPL, deferred in the originating request until `sumac ask` itself is proven.
- Test strategy for the agent loop without invoking a real model — the originating request calls for separating orchestration (sequencing tool calls, applying writes, rendering the proposal) from inference (the actual `Runner` call), so the orchestration can be driven by a fake model/fake tool-call sequence in tests.

## 7. How mistral.rs's Python SDK actually structures agentic tool use

Checked directly against two sources rather than assumed: the type stub shipped in the installed package (`.venv/lib/python3.12/site-packages/mistralrs/__init__.pyi`, version 0.9.2, matching this repo's pinned dependency) and the upstream docs and examples at `EricLBuehler/mistral.rs` on the `master` branch, fetched fresh rather than recalled from the originating request's pasted excerpts.

**Two distinct loops, named as such by the docs themselves** (`docs/src/content/docs/guides/agents/tool-calling-basics.mdx`, "The two modes"):

- **Client-side loop.** The model emits `tool_calls`; the caller runs the tool itself and sends the result back as a new `{"role": "tool", ...}` message in a second `send_chat_completion_request` call. Demonstrated in `examples/python/tool_call.py`, which calls `send_chat_completion_request` twice, manually appending both the assistant's tool-call message and the tool's result between calls.
- **Server-side loop.** mistral.rs runs the entire round-trip — inference, tool execution, feeding the result back, repeating — inside one `send_chat_completion_request` call. Demonstrated in `examples/python/agentic_tools.py` and `examples/python/custom_tool_call.py`, both of which register a `tool_callbacks` mapping on `Runner` and set `max_tool_rounds` on the request.

Both loops are real, current, and documented; "server-side" is the project's own name for this mode even in the Python SDK, where there is no literal server — it means "mistral.rs's own runtime drives the round loop," matching the same code path the HTTP `mistralrs serve --agent` mode uses. The distinction drawn in §2 above (in-process SDK agentic callbacks vs. the `mistralrs serve --agent` HTTP mode) is a different axis from this one — client-side vs. server-side loop is about *which loop drives the tool-call/result cycle*, and is orthogonal to *whether that loop runs in-process or over HTTP*. Both loops are equally in-process when driven through `Runner`; the server-side loop is the one that additionally needs `tool_callbacks` registered so mistral.rs has something to call.

**The server-side loop engages only when specific request conditions hold** (`agentic-runtime.md`, "How the loop runs"): `web_search_options` set, a `code_interpreter`/`shell` tool entry with the corresponding executor enabled, **"The request carries `tools` and server-side executors exist for them (SDK `tool_callbacks` or connected MCP tools)"**, `max_tool_rounds` set, or a `--tool-dispatch-url` configured server-side. For sumac, registering `tool_callbacks` on `Runner` and setting `max_tool_rounds` on the request both independently satisfy this — belt and braces, and consistent with every server-side-loop example in the repo, which sets both.

**Exactly one tool call executes per round, even if the model asks for several.** `agentic-runtime.md` states this plainly, as a numbered step in the loop description: "The tool is executed through one of the paths above... If the model returns more than one tool call, only the first is executed and a warning is logged." This is load-bearing for sumac's compound-request case (§2's ragu/tomatoes example): a model that emits four tool calls in one turn — hoping to consume the ragu, consume the tomatoes, discover ragu-for-one, and discover the tomato jar all at once — would have three of those four silently dropped, with only a log warning on mistral.rs's side. Getting all four writes to happen requires the model to make them across four separate rounds, each one seeing the previous round's result before deciding the next call — which is also how the loop is designed to work (step 6: "the message history is extended with the assistant's tool-call message and a `tool`-role response, so the next inference pass sees the outcome"). Two consequences for the design in §3/§9 below: `max_tool_rounds` must be sized for *one tool call per round, sequential*, not for "how many distinct actions this request needs" — the ragu/tomatoes example needs at minimum six rounds (two `find_inventory` calls plus four writes) before the model's final free-text reply, so a cap in the low single digits would truncate a real compound request without any error, only an incomplete result.

**`agent_permission` / `agent_approval_callback` gate registered `tool_callbacks`, not only mistral.rs's built-in tools.** `permissions-and-approvals.mdx` states the scope directly: "`agent_permission` controls whether mistral.rs may run an agent action after the model asks for one. It applies to all server-executed actions, not just Python: code execution, shell, web search, file tools, **registered callbacks**, and external tool dispatch." The only worked Python example (`examples/python/code_execution_approval.py`, also reproduced in the docs page's Python tab) demonstrates the mechanism against the built-in code-execution tool rather than a custom `tool_callbacks` entry, but the documented scope is explicit that it is the same mechanism for both. The callback receives an `AgentToolApproval` (`approval_id`, `session_id`, `round`, `tool: AgentToolMetadata` carrying `source`/`kind`/`label`, `arguments_json`, and a convenience `.arguments()` parse method) and returns `True`, `False`, or an `AgentToolApprovalDecision` (`.approve(remember_for_session=...)` / `.deny(message=...)`) — all present in the installed 0.9.2 stub (`AgentPermission`, `AgentToolApproval`, `AgentToolApprovalDecision`, `AgentToolMetadata`, `AgentToolSource`, `AgentToolKind`).

**`function.strict: true` constrains tool-call argument decoding; `grammar`/`grammar_type` constrains the final assistant message.** These are two separate mechanisms, not variants of one. `tool-calling-basics.mdx`'s "Strict tool calling" section: with `strict: true` on a tool's `function` object, "mistral.rs constrains the generated arguments to the tool's `parameters` JSON Schema during decoding... Non-strict tools are only parsed as JSON after generation completes, so malformed output can still appear." Built-in tools are strict by default; custom function tools are not, unless the schema sets it explicitly — every custom-tool example in the repo that defines its own function (`tool_call.py`, `agentic_tools.py`) sets `"strict": True` in the schema dict by hand. Separately, `grammar_type="json_schema"` plus a `grammar` JSON string constrains the assistant's own text output to a schema (`examples/python/pydantic_schema.py`, using `Fleet.model_json_schema()`; `examples/python/json_schema.py`, using a hand-written schema) — this is a `ChatCompletionRequest` field pair unrelated to `tool_schemas`/`tool_choice`, useful for structured final answers, not for tool-call arguments.

**The docs' own stated first preference for custom tools is MCP, not SDK callbacks.** `tool-calling-basics.mdx`: "For custom tools, the cleanest path is to run them as an MCP server and connect mistralrs as a client... The SDKs can also register custom callbacks directly: Python uses `Runner(tool_callbacks=...)`." Sumac's scope constraints (§ "Important scope constraint" in the originating request) explicitly rule out MCP. Recorded here so the design in §9 below is understood as picking the documented alternative to the maintainers' first-choice pattern, not as having missed it — `Runner(tool_callbacks=...)` is the correct in-process mechanism given that constraint, not merely *a* mechanism among equally-preferred options.

**The Python `ToolChoice` enum, as installed, has exactly two members.** `mistralrs/__init__.pyi`: `class ToolChoice(Enum): NoTools = "None"; Auto = "Auto"`. `tool-calling-basics.mdx`'s "Forcing or suppressing tool use" section documents additional `tool_choice` shapes — a named-function force, `"required"`, and `{"type": "allowed_tools", ...}` — but `ChatCompletionRequest.tool_choice` is typed `ToolChoice | None`, and the Python enum offers no constructor for those variants. Those shapes are demonstrated in the docs only for the HTTP/OpenAI-SDK/Rust surfaces (`tool-calling-basics.mdx`'s "Allowed Tools Examples" tabs show Python only via the *HTTP-facing* `openai` client, not the `mistralrs` Python SDK). Concretely: forcing the model to call `find_inventory` before it is allowed to answer, or restricting a given round to a named tool, is not reachable through the Python SDK's `tool_choice` field in 0.9.2 — only `Auto` (model decides) or `NoTools` (disabled) are available from Python. Steering "search before acting" (§3) is therefore a prompting concern, not an API-enforced one, for this SDK surface.

## 8. Is `AgentRunner`, as committed, the idiomatic shape?

Partially. The class-shaped wrapper itself is reasonable and matches how every example in §7 is structured: construct one `Runner` with `tool_callbacks` registered up front, closed over whatever state the callbacks need, then issue one `ChatCompletionRequest` per user turn against it. `cli.py`'s `AgentRunner(data_dir, key)` follows that shape at the construction boundary — `data_dir`/`key` are exactly what sumac's tool callbacks would need closed over to call into `ledger`/`decide`/`config`, and building the `mistralrs.Runner` once per `AgentRunner` (rather than once per tool call) is the right granularity given the getting-started guide's own note, quoted in the originating request, that a `Runner` should be "reuse[d]... for the lifetime of the process to avoid reloading."

Two parts of the committed shape are not idiomatic, for reasons grounded in §7 rather than style preference:

**1. `.run(prompt) -> str` gives no seam for the confirm step §5 requires, and per §7's server-side-loop mechanics, there may be no way to add one after the fact.** If `.run()` returns a single string and the CLI's only remaining step is `render.print_success(result)` (cli.py:398), the actual domain writes — the `decide.decide_change`/`store.append` calls a tool callback would make for `consume_inventory`/`move_inventory`/`discover_inventory` — must already have happened *inside* `.run()`, before it returns. But §7 established that the server-side loop executes each registered callback synchronously, one at a time, as soon as the model asks for it (or as soon as `agent_approval_callback` approves it, if permissioning is used) — there is no mode in which `send_chat_completion_request` collects every tool call the model would like to make across a whole session and hands them to the app as a batch before any of them run. A `.run()` returning one final string is only coherent if every write already committed one-by-one as the model proposed each one — which forecloses §5's "propose the whole plan, review it, then commit" behavior at the type-signature level, not just as an unfinished detail. Whatever confirmation mechanism sumac adopts has to be wired into the same call that runs the loop (see §9), not layered on afterward around a `str` return.

**2. `.run() -> str` discards `ChatCompletionResponse.agentic_tool_calls`.** The 0.9.2 stub declares `agentic_tool_calls: list[AgenticToolCallRecord] | None` on `ChatCompletionResponse`, populated by the server-side loop — one entry per round, carrying `round`, `name`, `arguments`, `result_content`. This is exactly the structured trace a confirmation UI, a `sumac ask --verbose` flag, or `render.py`'s existing table-building functions would want (`render.print_log`, `render.print_doctor` both render structured per-record tables today, not prose). Collapsing the response to `choices[0].message.content` before it reaches `cli.py` throws that trace away before the CLI layer ever sees it exists.

**3. Naming, minor.** `sumac.llm.AgentRunner` wraps a `mistralrs.Runner` under a name that differs by one word, inviting the reader to wonder whether it *is* one (subclass) or *holds* one (composition) without reading the implementation. Not load-bearing, noted for whoever writes `llm.py`.

None of this is a claim that `Runner(tool_callbacks=...)` itself, or a per-request `ChatCompletionRequest`, is the wrong mistral.rs primitive — §7 supports both as the documented, current, idiomatic mechanism for in-process custom tools. The critique is scoped to the thin wrapper `cli.py` already assumes exists (`AgentRunner(data_dir, key).run(prompt) -> str`), not to the underlying library usage that wrapper would presumably call into.

## 9. Recommended shape

Design-level only — no code follows from this section; `cli.py` is unchanged by this entry, per instruction.

**Confirmation mechanism: `agent_permission=AgentPermission.Ask` with a `agent_approval_callback` sumac supplies, rather than staging writes and asking once at the end.** §7 established that `agent_permission`/`agent_approval_callback` is documented to cover registered `tool_callbacks`, and it is the one piece of this problem mistral.rs already implements — the pause/resume bookkeeping around "ask before running this specific action" is the library's, not sumac's, to build. The callback would auto-approve `find_inventory` (read-only, matches §3's "search before acting" requirement — no reason to interrupt a lookup) and, for `consume_inventory`/`move_inventory`/`discover_inventory`, render `call.arguments_json` (or the parsed form via `call.arguments()`) as a human-readable line — product, quantity, unit, location — before asking. `AgentToolApprovalDecision.approve(remember_for_session=True)` gives an escape hatch matching the CLI approval example's own `"a"`/"always" option, letting a user fast-forward through the remaining writes in one compound request after confirming the first.

This is an explicit, acknowledged departure from §5's original sketch of one holistic preview covering the whole proposed transaction before any of it commits: what mistral.rs provides natively reviews each write in sequence, as the model proposes it, not the full batch upfront. The alternative that would preserve the original sketch exactly — mutating tool callbacks that only stage a proposed write (accumulated in a list the closure owns) without calling `store.append`, letting the whole tool-calling loop run to exhaustion untouched, then presenting the accumulated list once and replaying it for real only after a single confirm — was considered and set aside for this entry's recommendation, not because it is unworkable, but because it needs sumac to build bookkeeping mistral.rs doesn't provide: later tool calls in the same run (a second `find_inventory` checking whether an earlier proposed consumption already landed) would need to reason against a staged view — real inventory plus not-yet-committed proposed deltas layered on top — rather than `ledger.build_inventory`'s real result, or the model could reason from stale figures mid-plan. The per-call approval path avoids that because every approved write commits for real immediately, so every subsequent tool call already sees the true state. Recorded here as the rejected alternative rather than omitted, since it is the more faithful match to §5's original design and a future entry may revisit the tradeoff once the per-call approval path has been tried against real requests.

**Tool schemas set `"strict": true`**, per §7's finding that custom function tools are not strict by default and every worked example that defines one sets it explicitly — material for a model in the 0.6B–1.7B range, where malformed tool-call JSON is a real failure mode the strict-decoding constraint removes outright rather than something sumac's own code would need to detect and recover from after the fact.

**`max_tool_rounds` sized for one write per round, not one round per compound request** — per §7's "only the first tool call in a round executes" finding, a cap needs headroom for *every* sequential search-then-act step a compound request could produce, not just "the number of distinct actions" — the ragu/tomatoes example needs at least six successful rounds before a final reply. A cap that is too low does not error; it silently stops the plan partway through, which is worse than an explicit rejection given §3's "never invent, never guess" requirement — a user seeing only two of four intended writes complete, with no error, is the anomaly-must-be-loud principle (`docs/journal/2026-08-30_decide-pattern-data-integrity-upgrade.md` §3.2) failing at the agent layer instead of the fold layer.

**Search-before-act, one-action-per-turn, and ask-when-ambiguous are prompt content, not request parameters** — per §7's finding that the Python SDK's `ToolChoice` enum cannot force a specific tool or require tool use, these behaviors (already specified in §3 above) have no API-level enforcement available from this SDK surface and must be carried entirely by a system-role message. This is worth stating plainly because it changes what "done" looks like for whoever implements this: the system prompt is not incidental scaffolding around the real mechanism, it *is* the mechanism for these three behaviors, and needs the same testing attention as the tool schemas themselves.

**Model loading: `Which.GGUF(quantized_model_id=..., quantized_filename=...)` over `Which.Plain(...) + in_situ_quant=...`** for this specific use case. Both are legitimate, current, documented loading paths (the latter is the getting-started guide's own primary example); the recommendation for sumac specifically is that `Which.GGUF` selects an already-quantized checkpoint, avoiding a full-precision download before local requantization — material for a model whose whole reason for being chosen is to be the small, CPU-only, always-resident option. `Which.GGUF`'s fields (`quantized_model_id`, `quantized_filename`, `tok_model_id`, `tokenizer_json`, ...) are present in the installed 0.9.2 stub.

**`session_id` left unset**, consistent with §4's existing "not a chat product" position and confirmed rather than merely asserted by this research: `agentic-runtime.md` describes `session_id` as being for continuity *across separate requests* ("message history, tool records, media, and code-execution state" surviving between calls) — a single `sumac ask` invocation makes one `send_chat_completion_request` call, and that call's internal multi-round loop does not need a `session_id` to function; the field exists to let a *future* multi-turn surface (§6's deferred `sumac` REPL) resume state between separate top-level calls, which is exactly the point at which sumac would need to start passing one.

## 10. Open questions surfaced by this research, not resolved by it

- Whether `AgentToolApprovalDecision.approve(remember_for_session=True)` has any effect within the internal rounds of a *single* `send_chat_completion_request` call made without an explicit `session_id` — the docs describe `remember_for_session` and `session_id` primarily in terms of cross-request continuity, and it is not confirmed here whether "remember" has a session to scope to for the length of one multi-round call if no `session_id` was passed into that call. Matters directly for §9's recommended use of `remember_for_session` as a fast-forward inside one compound `sumac ask` invocation — if it does not carry across that call's own internal rounds without a passed `session_id`, either `sumac ask` needs to mint an ephemeral `session_id` per invocation (in tension with the "not a chat product," no cross-invocation continuity position in §4/§9), or the fast-forward escape hatch does not work as sketched and each write in a compound request is asked about individually regardless.
- Whether a system-role message is accepted by mistral.rs's Python SDK the same way `user`/`assistant`/`tool` roles are in the fetched examples — inferred from OpenAI wire-format compatibility (every example's `messages` list follows that shape) rather than confirmed by a worked example containing one; none of the examples fetched for this entry happen to include a system message.
- Whether `agent_approval_callback`'s `AgentToolMetadata.kind` for a registered `tool_callbacks` entry is `AgentToolKind.Custom`, `.External`, or something else — the enum has both variants and no fetched example exercises the approval callback against a custom (non-built-in) tool to show which one a `tool_callbacks`-registered function actually reports.

## 11. Preview / dry-run mode, and why it is scoped to `sumac ask` alone

A separate requirement from §5's confirmation step: a mode that computes what `sumac ask` would do and never commits it, regardless of any answer a human would give — for inspecting or testing a request's interpretation without touching the vault at all. The originating request for this addition considered and rejected generalising this to every CLI command (a `--dry-run` flag on `sumac add`/`sumac snapshot`/etc.), on the grounds that producing the sequence of tool calls (`sumac ask`'s own job) is a separate concern from whether an individual already-fully-specified command like `sumac add` supports a dry run. That scoping is also the cheaper one to build: `decide.decide_change` and `decide.decide_correct` are already the seam a dry run needs, and already exist. `decide.decide_change` is documented as pure — "no I/O, no clock, no filesystem. State and config are arguments" (`docs/journal/2026-08-30_decide-pattern-data-integrity-upgrade.md` §3.5) — and confirmed structurally: it returns `tuple[list[Write], list[str]]` (decide.py:476-488) and touches neither `store` nor the filesystem anywhere in its body. The only place a `sumac add` invocation actually mutates anything is `cli.py`'s own `add` command, in the loop `for w in writes: store.append(data_dir, key, w.stream, w.obj)` (cli.py:264-265), *after* `decide_change` has already returned. A dry run for `sumac ask` therefore costs nothing at the domain layer: a mutating tool callback (`consume_inventory`, `move_inventory`, `discover_inventory`) calls `decide.decide_change` exactly as `cli.py`'s `add` command does today, and a dry run is simply the callback choosing not to take the second step — the `store.append` loop — that `cli.py` currently takes unconditionally. No change to `decide.py`, `store.py`, or any other CLI command follows from this. `sumac add` itself gains nothing and needs nothing.

## 12. Update to §9: preview-then-accept, not per-write approval, as the primary mechanism

§9 recommended `agent_permission=AgentPermission.Ask` with a per-tool-call `agent_approval_callback` as the confirmation mechanism, explicitly trading away §5's original "review the whole plan once" sketch because building that required a staged-inventory overlay (§9's rejected alternative) that mistral.rs does not provide. §11's dry-run mechanism removes the reason that alternative was rejected: it does not need a staged-inventory overlay, because in dry-run mode nothing commits at any point, so there is nothing for a later tool call in the same run to observe having changed. The dry run's whole point *is* to compute the plan without touching real state — the staged-overlay problem only mattered for the previously-considered version of that alternative, where writes needed to commit for real as soon as approved so that later tool calls saw a consistent live inventory. A dry run sidesteps that requirement by never committing at all, in either the approved or unapproved case, until a separate step after the model is no longer involved (§14).

**Revised recommendation: the whole-plan preview from §11 is the primary interaction, not `agent_approval_callback`.** The default (non-flag) `sumac ask "<sentence>"` invocation runs the agentic loop once in dry-run mode — `agent_permission=AgentPermission.Auto`, since nothing destructive is wired up regardless of permission mode when every mutating tool callback stops short of `store.append` — collects the resulting proposed plan, and only asks the human after the model has finished proposing, not before each individual write. `agent_permission=Ask`/`agent_approval_callback` is not required by this shape and is demoted from "the mechanism" to an optional defense-in-depth layer that could be added later around whatever eventually performs the real `store.append`; the default recommended design does not need it, because §14 has the human review the complete, already-fully-decided plan before any model or tool-callback code runs a second time.

This also moots, rather than answers, §10's first open question (`remember_for_session` without an explicit `session_id`): §14 mints an explicit `session_id` for other reasons (the feedback loop, §13), so a session unambiguously exists for the length of one `sumac ask` invocation regardless of what `remember_for_session` alone would or would not scope to — the design no longer depends on that question's answer.

## 13. Feedback and iterative revision

The requirement that prompted this update: after a preview, the human should be able to respond with more than accept or reject — free text describing what to change — and get back a revised plan, not just exit. Two ways to give the model that history were available from §7's research, and only one is a good fit:

**Rejected: sumac reconstructs the message history itself and resends the whole thing.** `ChatCompletionResponse` carries `agentic_tool_calls: list[AgenticToolCallRecord]` (round, name, arguments, result_content) but no `messages` field — the expanded per-round assistant/tool messages the server-side loop actually produced internally are not handed back to the caller. Rebuilding them well enough to append a correction and resend would mean sumac reconstructing tool-call IDs and message shapes for the model's own chat template, matching a wire format sumac does not own — the getting-started guide's "not a chat product" position (§4) is about not accumulating a *persisted* conversation across separate CLI invocations; it says nothing against a library-native mechanism for continuing one conversation within a single invocation's lifetime, and reconstructing that mechanism by hand duplicates something the SDK already does more reliably.

**Adopted: an ephemeral, per-invocation `session_id`.** `agentic-runtime.md`: "Use `session_id` when your app needs continuity across requests: message history, tool records, media, and code-execution state." `sumac ask` mints one `session_id` (e.g. `str(uuid4())`) when the command starts, holds it only in process memory, and passes it on every `ChatCompletionRequest` for the duration of that one invocation — never written to disk, never read back on a later `sumac ask` call, so the "not a chat product" position from §4 is preserved at the granularity that matters: no history persists *between* invocations, only *within* one. The feedback loop becomes: append the human's typed correction as a new `{"role": "user", "content": ...}` message, call `send_chat_completion_request` again with the same `session_id`, and let mistral.rs's session state supply everything the model already knows about the original request and what it already proposed — sumac does not need to re-explain any of it. Sumac's own accumulated-plan list (§14) is cleared before each such call: a revision replaces the previous proposal rather than appending to it, since the model is being asked to reconsider the whole plan, not extend it.

**No cap on the number of feedback rounds.** An earlier draft of this section proposed a small fixed cap on how many times the human could give feedback in one session, reasoned about as guarding against "an unbounded back-and-forth." That reasoning does not hold up: every round of feedback is the human spending their own local time and their own machine's CPU, at no cost to anything else, and no other command in this CLI is designed to stop responding to its own user after a fixed number of tries — `sumac correct` does not refuse a third correction, `sumac add` does not refuse a third attempt at an invocation it rejected twice. There is one real cap in this design, and it applies to something else entirely: `max_tool_rounds` (§9, §14 step 2), which bounds how many sequential tool calls the *model* can make while producing a single preview or a single revision. That cap exists to guarantee the call terminates — a model that never stops calling tools would otherwise hang the process — not to ration how much of the model's or the CLI's own time the human is allowed to use. `max_tool_rounds=20` is recommended as that termination guarantee: generous relative to §2's worked example (two searches plus four writes, six rounds), enough headroom for a request several times more compound before the cap is what stops a real request rather than a runaway loop.

**A third, distinct cap: `self_review_rounds`, model-initiated rather than human-initiated.** Separate from both of the above is a self-review step — the model checking its own just-produced plan against the original sentence, revising if it finds a mismatch, before a human ever sees it. Mechanically this is the same continuation pattern as the human feedback loop (§14 step 4's "Feedback" branch): clear the accumulated list, send a message on the same `session_id`, let the model respond, replace the plan if it changed. The difference is who writes the message and who decides whether to do it again — human feedback is typed by a person who chooses each time whether it is worth another round; a self-review pass is a fixed message sumac sends itself (e.g. "Check the plan above against the original request. If it is correct, say so with no further tool calls. If not, revise it.") with nothing external gating whether it happens again. That absence of a human decision on each round is exactly why this one needs a cap and the feedback loop does not: an ungated automatic loop risks not converging — a small model oscillating between two plans rather than settling on one is a real, known failure mode, and each pass is a full extra local-inference round-trip the human is waiting on before seeing anything at all. Recommended default: `self_review_rounds=1` — one self-check pass, not zero (skips the check entirely) and not several (compounding latency against uncertain benefit for a model in the 0.6B-1.7B range, where confidence in self-critique quality is weakest). Configurable down to `0` (disabled, the plan from step 3 goes straight to the human) per the same "put model configuration somewhere sensible, changeable without touching agent logic" requirement §3/the originating request already states for model choice generally.

## 14. Concrete interaction shape

Design-level only, in the same sense as §9 — no code follows from this section.

1. `sumac ask "<sentence>"` mints `session_id = str(uuid4())`, kept in memory only.
2. Sumac builds one `ChatCompletionRequest` — the system-role prompt from §9 plus the user's sentence, `tool_schemas` with `strict: true` per §9, `tool_choice=ToolChoice.Auto`, `max_tool_rounds` sized per §9's "one write per round" finding, `agent_permission=AgentPermission.Auto`, `session_id=session_id` — and calls `send_chat_completion_request`. Every mutating tool callback (`consume_inventory`/`move_inventory`/`discover_inventory`) calls the matching `decide.*` function against the real, current `ledger.build_inventory`/`config.build_config` state (so the plan reflects genuinely valid operations, warnings and all — a query that would raise `Rejected` still raises it here, surfacing the domain error to the model exactly as §3 already specifies) but does not call `store.append`; instead it appends the resolved call — which `decide.*` function, and the exact keyword arguments used to call it — to a plain list closed over by the orchestration layer, and returns a description of the resolved effect to the model as the tool result (so the model's own subsequent reasoning proceeds as if the write had happened).
3. When the call returns — the model produced a final reply with no further tool call, or `max_tool_rounds` was reached — and the accumulated list is non-empty (§13's self-review has nothing to check for a read-only request, so this step is skipped when the model made no mutating tool calls), sumac runs up to `self_review_rounds` self-review passes (§13): send the fixed self-check message on the same `session_id`, and if the model's response contains new tool calls, clear and replace the accumulated list and repeat, up to the configured limit; a response with no new tool calls ends the loop early, before the limit.
4. Sumac renders the accumulated list. An empty list (a read-only request like "where is the jam?") skips straight to printing the model's final text and exiting; there is nothing to accept or reject. A non-empty list is rendered as a structured plan — reusing `render.py`'s existing table-building style (`print_log`, `print_doctor`) rather than the single `print_success(str)` call `cli.py`'s current stub uses (§8).
5. For a non-empty plan, sumac prompts for one of: accept, reject, or free-text feedback.
   - **Reject**: print nothing further, exit 0, nothing written. The `session_id` is discarded with the process.
   - **Feedback**: clear the accumulated list, append the typed text as a new user message, call `send_chat_completion_request` again with the same `session_id`, and go to step 3 with the new response. Not capped (§13) — the human may give feedback as many times as they choose; only `max_tool_rounds=20` (per call, not per session) bounds any single one of these calls, as a termination guarantee rather than a limit on how many times the loop may run.
   - **Accept**: for each resolved call recorded in step 2, in the same order, call the matching `decide.*` function *again* — same keyword arguments, but against a *freshly reloaded* `ledger.build_inventory`/`config.build_config` and a fresh `occurred_at=datetime.now(UTC)` — rather than replaying the `Write` objects computed during the dry run. Then `store.append` each resulting write, printing any messages `decide.*` returns exactly as `cli.py`'s `add` command already does (cli.py:262-263).
6. `--dry-run` (naming: the user's own "preview" and the more conventional Unix "dry run" name the same thing here, treated as synonyms in this entry) runs steps 1-4 only and exits — self-review (step 3) still runs, since its purpose is plan quality and a dry run's whole output *is* the plan, but there is no accept/reject/feedback prompt (step 5), since there is nothing to act on the answer to. Useful non-interactively (no TTY to prompt against) and for iterating on a request's wording before ever risking a real commit.

Re-deciding on accept rather than replaying the dry run's `Write` objects verbatim is deliberate, not an oversight: `decide.decide_change`'s stock-shortfall handling (`docs/journal/2026-08-30_decide-pattern-data-integrity-upgrade.md` §3.5, "the shelf is authoritative, not the log") exists precisely to reconcile a decision against whatever the real holdings are *at commit time* — real time passes between a preview and an accept (at minimum, the time spent reading the preview; more if a feedback round happened), and something else could have changed the vault in between. Replaying stale `Write` objects computed during the dry run would bypass that reconciliation and could commit a `Counted` correction sized for inventory levels that no longer hold; re-invoking `decide.*` with the same arguments against current state gets the shelf-is-authoritative behaviour for free, the same way it already works for `sumac add`.

## 15. Limitations this design accepts rather than solves

- **A dry run's own tool calls do not see each other's not-yet-committed effects.** If a compound request's plan includes a second `find_inventory` search against a product or location an earlier step in the *same* dry run already proposed changing, that search still reads real, on-disk state — nothing was written, so there is nothing different for it to see. §9's rejected staged-inventory-overlay alternative would have solved this (and its counterpart for a live per-write-approval design), and remains available to build later if a real compound request is found to need it; it is not built for this entry's recommendation, on the grounds that no example so far (§2's ragu/tomatoes case included) actually re-queries a product it has already proposed changing within the same request.
- **A revision (§13) discards the previous proposal rather than diffing against it.** The human sees a full new plan after feedback, not a highlighted delta from the one before. Acceptable for a first pass; worth revisiting if compound requests commonly need several small corrections rather than one.

## 16. Implementation handoff

§1-§15 record how this design was reached and why. What follows is written for an implementing agent starting cold, with no memory of the conversation that produced it — everything it needs to act on is restated here rather than referenced back. It is a specification of shapes, contracts, and content (tool schemas, class interfaces, prompt text, error mapping) — not a sequence of edit steps. Nothing in §16-§24 has been written to code; `cli.py`, `decide.py`, `ledger.py`, `config.py`, `models.py`, `render.py`, and `store.py` are exactly as described in the Current State section below, unchanged by this entry.

**Module.** `cli.py`'s already-committed `ask` command (cli.py:373-404) imports `from sumac import llm` and calls `llm.AgentRunner(data_dir, key)`. That fixes the module's name (`sumac/llm.py`, a new file) and the entry class's name (`AgentRunner`) as a starting constraint, not a free choice — changing either means also changing `cli.py`'s import and call site, which this entry does not do. §19 below gives `AgentRunner` a richer interface than the single `.run(prompt) -> str` method `cli.py` currently calls; reconciling `cli.py`'s call site with that richer interface (or preserving `.run()` as a thin compatibility wrapper around it) is left to the implementing agent, called out again in §24.

**Tool surface: four tools, not the three named in the MVP scope, and why.** The originating request's "MVP tools" list names three: find/search, consume, move. §2's reference example — the ragu/tomatoes request already committed to throughout §3 and §14 — needs a fourth: after the ragu is consumed from the freezer, it comes back as "ragu for one" in a Tupperware box; after the tin of tomatoes is consumed from the pantry, it comes back as a jam jar of tomatoes in the fridge. Neither can be expressed as a `move`: `models.InventoryChange`'s own shape ties a `MOVEMENT` to one `product_id`, one `Quantity`, moving from one `from_location` to one `to_location` (models.py:73-93) — there is no field for the product or unit changing in transit, because a movement is not a transformation. The fourth tool, `discover_inventory`, maps to `ChangeKind.DISCOVERY` (models.py:31), already exactly the "this new thing now exists here, with no claim about where it came from" event `sumac add discovery` uses today. This is a decision made by this entry, not left open: four tools, because the worked example the whole design has been validated against needs all four, and `purchase`/`waste`/`correction` remain out of scope exactly as the originating request specified (`decide.decide_correct` is not exposed as a tool at all in this design).

## 17. Tool schemas

All four as OpenAI-style function schemas, each with `"strict": true` (§7, §9 — required explicitly; not on by default for a custom tool). `amount` is typed `"string"`, not `"number"`, deliberately: `cli.py`'s own `add` command takes `amount: str` and parses it through `_parse_decimal` (cli.py, `_parse_decimal`) rather than accepting a numeric type, because `Decimal` needs an exact decimal string and JSON's number type does not guarantee one — the tool schemas keep the same convention `sumac add` already uses rather than introducing a second, JSON-number-based one.

```json
{
  "type": "function",
  "function": {
    "name": "find_inventory",
    "description": "Search current inventory by product name (partial, case-insensitive match). Returns every location currently holding a matching product, with its exact product_id, location_id, amount, and unit as sumac has them recorded. Call this before consuming, moving, or discovering any product — never guess a product_id, location_id, amount, or unit that has not been returned by this tool.",
    "strict": true,
    "parameters": {
      "type": "object",
      "properties": {
        "query": {"type": "string", "description": "Product name or partial name, e.g. \"ragu\" or \"jam\"."}
      },
      "required": ["query"],
      "additionalProperties": false
    }
  }
}
```

```json
{
  "type": "function",
  "function": {
    "name": "consume_inventory",
    "description": "Propose recording that some quantity of a product was used, eaten, or consumed from one location. product_id, unit, and from_location must be exact values already seen in a find_inventory result — never invented. Does not immediately alter inventory; the proposal is shown to the person for review before anything is written.",
    "strict": true,
    "parameters": {
      "type": "object",
      "properties": {
        "product_id": {"type": "string"},
        "amount": {"type": "string", "description": "Plain decimal string, e.g. \"1\" or \"0.5\"."},
        "unit": {"type": "string"},
        "from_location": {"type": "string"}
      },
      "required": ["product_id", "amount", "unit", "from_location"],
      "additionalProperties": false
    }
  }
}
```

```json
{
  "type": "function",
  "function": {
    "name": "move_inventory",
    "description": "Propose recording that some quantity of a product was relocated from one location to another, unchanged — same product, same unit, only the location differs. If the product's identity, container, or unit changed (cooked, decanted, repackaged), use discover_inventory for the result instead of move_inventory.",
    "strict": true,
    "parameters": {
      "type": "object",
      "properties": {
        "product_id": {"type": "string"},
        "amount": {"type": "string"},
        "unit": {"type": "string"},
        "from_location": {"type": "string"},
        "to_location": {"type": "string"}
      },
      "required": ["product_id", "amount", "unit", "from_location", "to_location"],
      "additionalProperties": false
    }
  }
}
```

```json
{
  "type": "function",
  "function": {
    "name": "discover_inventory",
    "description": "Propose recording that some quantity of a product now exists at a location, with no claim about where it came from. Use this for a product whose identity, container, or unit changed since it was taken from inventory — a cooked dish, a decanted portion, a repackaged remainder — where the result cannot be expressed as a move. product_id may be a new name distinct from any product consumed earlier in this request.",
    "strict": true,
    "parameters": {
      "type": "object",
      "properties": {
        "product_id": {"type": "string"},
        "amount": {"type": "string"},
        "unit": {"type": "string"},
        "to_location": {"type": "string"}
      },
      "required": ["product_id", "amount", "unit", "to_location"],
      "additionalProperties": false
    }
  }
}
```

Each tool's required-field set matches `decide._ENDPOINT_SHAPE` (decide.py:42-48) for the `ChangeKind` it produces: `CONSUMPTION` is `(True, False)` — `from_location` required, `to_location` absent; `MOVEMENT` is `(True, True)` — both required; `DISCOVERY` is `(False, True)` — `to_location` required, `from_location` absent. This is not a coincidence to preserve by hand: it is the same shape constraint `decide._check_endpoint_shape` (decide.py:64) already enforces on the domain side, expressed a second time in the tool schema so a malformed call is rejected by strict-mode decoding before it ever reaches `decide.decide_change`.

## 18. Tool callback behavior and result shapes

`find_inventory`'s callback calls `ledger.build_inventory(data_dir, key)` and `ledger.load_locations_or_empty(data_dir, key)`, then filters `inventory.by_location` the same way `render.print_find` already does (render.py:169-190: substring match, case-insensitive, `product_id.lower() in pid.lower()`) rather than inventing a second matching rule. Returns a JSON string: `{"matches": [{"product_id": ..., "location_id": ..., "location_path": ..., "amount": ..., "unit": ...}, ...]}`, `location_path` via `config.location_path(locations, loc_id)` (config.py:256) so the model sees the same human-readable path `sumac find` shows, and an empty `matches` list (not an error) when nothing matches — mirroring `render.print_find`'s own "not found" case, which is not treated as failure there either.

Each mutating tool's callback (`consume_inventory`/`move_inventory`/`discover_inventory`) parses `amount` via the same decimal-parsing rule `cli.py`'s `_parse_decimal` uses (cli.py, `_parse_decimal` — `Decimal(raw)`, catching `InvalidOperation`), builds the matching `ChangeKind` (`CONSUMPTION`/`MOVEMENT`/`DISCOVERY`), and calls `decide.decide_change` with that `kind` plus the tool's other arguments passed straight through as `product_id`/`amount`/`unit`/`from_location`/`to_location`, and `actor=paths.current_user()`, `occurred_at=datetime.now(UTC)`, `inventory=ledger.build_inventory(data_dir, key)`, `cfg=config.build_config(data_dir, key)` — the same four values `cli.py`'s `add` command already supplies (cli.py:246-249). Two outcomes:

- **`decide_change` returns `(writes, messages)` without raising.** The callback does not call `store.append` on `writes` (§11 — that is what makes this a dry run). It appends one `ProposedWrite` (§19) built from the *resolved* call — the same `kind`/`product_id`/`amount`/`unit`/`from_location`/`to_location` just passed to `decide_change`, plus `messages` as `warnings` — to the orchestration's accumulated list, and returns a JSON string tool result describing the proposed effect as if it had happened: `{"status": "proposed", "product_id": ..., "amount": ..., "unit": ..., "from_location": ..., "to_location": ..., "warnings": [...]}`, so the model's own subsequent reasoning (e.g. a later step in the same compound request) proceeds on the assumption the write will land, consistent with §3's "use the actual results returned by Sumac."
- **`decide_change` raises `Rejected`.** Nothing is appended to the accumulated list. The callback catches it and returns a JSON string tool result — `{"status": "rejected", "reason": e.reason, "detail": {k: str(v) for k, v in e.detail.items()}}` (`errors.Rejected.detail` is `dict[str, object]` and can hold non-JSON-serializable values such as `Decimal`; stringify every value before `json.dumps`, the same coercion `Rejected.__str__` already performs for its own message, errors.py:44) — back to the model as the tool result, per §3's "return the domain error to the model so it can recover or explain the problem," not as a raised exception that would end the request.

No other exception type is expected from a mutating callback under normal operation — `decide.decide_change`'s own docstring names `Rejected` as the only thing it raises (decide.py, `decide_change` docstring). Anything else escaping a callback is a bug in the callback or in sumac's own domain code, not a modeled outcome, and should not be caught and stringified the same way — see §23.

## 19. Orchestration interface

Three small types in `sumac/llm.py`, none of which exist yet:

```python
@dataclass(frozen=True, slots=True)
class ProposedWrite:
    kind: ChangeKind
    product_id: str
    amount: Decimal
    unit: str
    from_location: str | None
    to_location: str | None
    warnings: tuple[str, ...] = ()


@dataclass(frozen=True, slots=True)
class AgentPlan:
    reply_text: str
    writes: tuple[ProposedWrite, ...]
```

`AgentPlan` is what §14's steps 3-5 render and act on — the single object a dry run, a preview, or a revision produces. `AgentRunner` is the class `cli.py` already names:

```python
class AgentRunner:
    def __init__(self, data_dir: Path, key: bytes) -> None: ...
    def propose(self, prompt: str) -> AgentPlan: ...
    def revise(self, feedback: str) -> AgentPlan: ...
    def commit(self, plan: AgentPlan) -> list[str]: ...
```

`__init__` constructs the `mistralrs.Runner` once (§21) with `tool_callbacks` bound to the four callbacks in §18, closed over `data_dir`/`key`. `propose` mints the per-invocation `session_id` (§13), sends the system prompt (§20) plus the user's sentence, runs up to `self_review_rounds` self-review passes internally (§13, §14 step 3) before returning, and returns the resulting `AgentPlan` — callers do not see the self-review passes happen. `revise` sends `feedback` as a new user message on the same `session_id` (§13), also running its own internal self-review passes, and raises if called before `propose` (no session to continue). `commit` is §14 step 5's "Accept" branch as a method: for each `ProposedWrite` in `plan.writes`, in order, call `decide.decide_change` again with the same resolved arguments against a freshly reloaded `Inventory`/`Config` and `occurred_at=datetime.now(UTC)` (§14's "re-decide, don't replay" reasoning), then `store.append` each resulting write, and return one human-readable summary string per write — `f"Recorded {kind.value} of {amount} {unit} {product_id}"`, matching `cli.py`'s own `add` command's success message exactly (cli.py:266) rather than inventing new wording for the same fact.

This supersedes the `.run(prompt) -> str` contract `cli.py`'s current stub calls (§8's critique) — reconciling the two is explicitly left open, §24.

## 20. System prompt

A draft, not a final wording — needs tuning against an actual model's actual tool-calling behavior, which this entry has not done (§10, §16's own framing). Given as a concrete starting point rather than left unstated, since §7 found no API-level lever for any of the three behaviors it encodes (search-before-act, one tool call at a time, ask rather than guess):

```text
You are the tool-calling layer for a household grocery inventory. You can only
act through the four tools you are given: find_inventory, consume_inventory,
move_inventory, and discover_inventory. You have no other capability — no
filesystem, no shell, no network, no code execution.

Before consuming, moving, or discovering any product, call find_inventory to
get its exact product_id, location_id, amount, and unit. Never invent a
product_id, location_id, amount, or unit — use only what a tool result has
just given you.

If find_inventory returns more than one plausible match for what the person
asked about, and nothing in their request distinguishes which one they mean,
stop and ask them in plain text which one they mean, with no further tool
call, rather than guessing.

Call one tool at a time. After each tool call you will see its result before
deciding the next one — use that result, do not assume what it will be in
advance.

If a product changed identity, container, or unit since it was taken from
inventory (cooked, decanted, repackaged), record the result with
discover_inventory, not move_inventory — move_inventory is only for the same
product and unit changing location.

If a tool result has status "rejected", the action was not valid — explain
why in plain text, or try a corrected call if the correction is obvious from
the rejection reason, rather than repeating the same call unchanged.

When you have made every tool call the request needs, or if the request
needs no tool call at all (a plain question), respond in plain text with no
further tool calls.
```

Whether this is passed as a `{"role": "system", ...}` message is itself unconfirmed against a worked mistral.rs example (§10, §7) — assumed from OpenAI wire-format compatibility, to be verified against the actual chosen model's chat template once one is selected (§21).

## 21. Model loading and configuration

`Which.GGUF(quantized_model_id=..., quantized_filename=...)` over `Which.Plain(...) + in_situ_quant=...`, per §9's reasoning (avoids downloading an fp16 checkpoint before local requantization). No specific repository or filename is chosen by this entry — §9 named Qwen3 in the 0.6B-1.7B range as the class of model to try first, following mistral.rs's own GGUF documentation example (which uses an `unsloth/Qwen3-0.6B-GGUF`-style repository), but this entry has not run that model against the tool schemas in §17 and cannot confirm it calls tools reliably at that size. Selecting and recording a specific `quantized_model_id`/`quantized_filename` pair that has been empirically checked against §2's reference example is left to the implementing agent (§24).

Getting the first model weights onto the machine needs a network fetch from Hugging Face's cache resolution — the getting-started guide's own words: "The first run downloads the weights into the Hugging Face cache." This is a one-time asset fetch, not a cloud inference call, but it is still network activity the first time `sumac ask` (or `--dry-run`) runs on a machine, worth a visible message before `Runner` construction begins (e.g. "Downloading <model> (first run only)...") rather than a silent multi-minute pause — no other sumac command touches the network at all, so a `sumac ask` first-run pause with no explanation would look like a hang.

Configuration values named by this entry so far — the model identifier/filename, `max_tool_rounds=20` (§13), `self_review_rounds=1` (§13) — are recommended to live as named constants at the top of `sumac/llm.py`, matching how `paths.py` keeps its own fixed values as module-level constants (`VAULT_FILENAME`, `CONFIG_FILENAME`, `LOG_DIRNAME`, paths.py:14-16) rather than scattering them through function bodies. An env-var override (matching `DataDirOption`'s existing `envvar="SUMAC_DATA_DIR"` pattern, cli.py:39-40) is a reasonable later addition, not required for a first pass.

## 22. Testability

`mistralrs.Runner` is a compiled extension type (`mistralrs.abi3.so`, confirmed by the installed package's file layout) — it cannot be instantiated cheaply or driven deterministically in a unit test the way `Config`/`Inventory` can. `AgentRunner`'s constructor should accept its underlying runner through a narrow, structurally-typed interface rather than importing and depending on `mistralrs.Runner` concretely everywhere it's used:

```python
class SendsCompletions(Protocol):
    def send_chat_completion_request(
        self, request: mistralrs.ChatCompletionRequest, model_id: str | None = None
    ) -> mistralrs.ChatCompletionResponse: ...
```

Production code passes a real `mistralrs.Runner` (which already satisfies this shape structurally); tests pass a small hand-built fake whose `send_chat_completion_request` returns canned response objects (or minimal stand-ins exposing the same `.choices[0].message.{content,tool_calls}` attributes `AgentRunner`'s code actually reads) to drive the tool-callback dispatch path deterministically — no real model, no real inference, no GGUF download — while still exercising real `decide.decide_change`/`store.append` calls against a real encrypted `data_dir`/`key` pair, using the same `data_dir`/`key`/`osuser` fixtures every other test module already uses (tests/conftest.py). This mirrors the originating request's own requirement, restated in §6: "separating orchestration... from inference... so the orchestration can be driven by a fake model/fake tool-call sequence in tests." A new `tests/test_llm.py`, following the existing one-test-file-per-module convention (`tests/test_decide.py`, `tests/test_ledger.py`, ...), is where this belongs; none exists yet.

## 23. Error-handling boundary

Two different places an exception can occur, and they should not be handled the same way. §18 already draws this line for `Rejected` specifically; restated here as the general rule, because it is exactly what the already-committed `cli.py` stub gets wrong (Stubbed, below): a `Rejected` raised *inside* a tool callback, while the model is still proposing (`propose`/`revise`, §19), is expected and recoverable — caught, turned into a `{"status": "rejected", ...}` tool result, and handed back to the model, never propagated as a Python exception out of `send_chat_completion_request`. A `Rejected` (or any other `SumacError`) raised during `commit` (§19), after the human has already reviewed and accepted the plan, is not a modeled outcome the model gets a chance to react to — the human is no longer in a position to revise anything, the loop is over — and should propagate normally, the same way `cli.py`'s `add` command already lets `decide_change`'s `Rejected` propagate to `cli.main`'s existing top-level handler (cli.py, `main`) rather than being caught and reworded. `cli.py`'s current `ask` stub's blanket `except Exception` (cli.py:402-404) does not draw this distinction at all — it would catch and reword a `commit`-time `Rejected` exactly the way it catches a genuine bug, which is the thing to specifically not carry forward into whatever replaces this stub (§24).

## 24. Left to the implementing agent

Restated from where each was first raised, gathered here as the working list:

- Reconciling `cli.py`'s existing `AgentRunner(data_dir, key).run(prompt) -> str` call site with §19's richer `propose`/`revise`/`commit` interface — including where the interactive accept/reject/feedback loop (§14) itself lives: as logic inside `cli.py`'s `ask` command, or as a separate function in `llm.py` that `cli.py` calls into, matching the existing pattern of keeping `cli.py` thin and pushing logic into dedicated, independently-testable modules (`decide.py`, `render.py`) (§8, §19).
- A specific, empirically-checked `quantized_model_id`/`quantized_filename` pair (§21) — not chosen by this entry, only the class of model and the loading mechanism.
- Tuning §20's system prompt against that model's actual behavior on §2's reference example and on simpler single-write requests.
- Confirming a system-role message is honored by mistral.rs's Python SDK the way `user`/`assistant`/`tool` roles are (§10, §20) — unverified here.
- Confirming `agent_approval_callback` gates a `tool_callbacks`-registered tool the way `permissions-and-approvals.mdx`'s prose says it does (§10) — moot for the recommended default design (§12 demotes it to optional, unused), but relevant again if defense-in-depth per-write approval is ever added around `commit` (§12).
- Whether `--dry-run` needs a short flag alias, and the exact flag/option names for accept/reject/feedback at the interactive prompt (§14 describes the three outcomes, not the literal prompt text or keybindings).
- Rendering: a concrete `render.py` addition (or a new function in `llm.py`) for an `AgentPlan`'s table, in the style of `print_log`/`print_doctor` (§14 step 4, §18) — no such function exists yet, and its exact columns/layout are not specified here beyond "structured, not a single string."

## 25. §9/§12 superseded: the server-side loop's Python bindings can't carry a real tool schema

§16-§24 were implemented as written, including §9's `tool_callbacks` registered on `Runner` and §12/§13's `agent_permission=AgentPermission.Auto`, `max_tool_rounds`, and a per-invocation `session_id` on every `ChatCompletionRequest`. The first real run — `unsloth/Qwen3-1.7B-GGUF` via mistral.rs 0.9.2, the class of model and the loading mechanism §21 recommended — failed on the very first tool-using request with `Tool 'find_inventory' conflicts with a registered internal tool. Internal tool names cannot be overridden.` Renaming all four tools to a `sumac_`-prefixed namespace (`sumac_find_inventory`, and so on — ruling out a collision with a specific reserved name) reproduced the identical error under the new names, which is what motivated tracing the actual cause rather than continuing to guess at names.

Traced against `EricLBuehler/mistral.rs`'s `master` branch (fetched fresh, not recalled from §7's earlier research): `mistralrs-pyo3/src/lib.rs`'s `wrap_tool_callbacks` — the function backing the Python SDK's `Runner(tool_callbacks=...)` — calls `builder.with_tool_callback(name, cb)`. `mistralrs/src/builder_macros.rs` defines that method to register each callback against an empty `Tool` (`function.description: None`, `function.parameters: None`, `function.strict: None`) — a bare name, nothing else. A second method on the same builder, `with_tool_callback_and_tool(name, callback, tool)`, does accept a real `Tool` definition, but the Python bindings never call it — only the schema-less variant is reachable from `Runner(tool_callbacks=...)`. Separately, `mistralrs-core/src/engine/agentic_loop.rs`'s `agentic_loop` function opens by rejecting, before any inference happens, every tool in the request's own `tools` (`ChatCompletionRequest.tool_schemas`, in the Python SDK's terms) whose name is already a key in `tool_callbacks` — the exact `"conflicts with a registered internal tool"` message, where "internal" means "already registered via `tool_callbacks`," not "one of mistral.rs's own hardcoded tools." Combined, these two facts mean the Python SDK's `tool_callbacks` path has no way to carry a real, `strict`-mode schema for a registered tool: registering it gives it an empty schema, and separately declaring `tool_schemas` for the same name to supply a real one is rejected outright. `examples/python/agentic_tools.py`, fetched fresh from `master` rather than assumed from §7's citation of it, does both — registers `tool_callbacks` and passes matching `tool_schemas` for the same two tool names (`get_weather`, `calculate`) — so this is a defect reachable from mistral.rs's own shipped example against 0.9.2, not something particular to sumac's tool names, request shape, or the `sumac_` rename.

The implemented `sumac/llm.py` uses the other loop §7 already established as real, current, and documented — the client-side loop (`examples/python/tool_call.py`): `Runner` is constructed with no `tool_callbacks` argument at all (llm.py:329-346); `ChatCompletionRequest.tool_schemas` still carries §17's four schemas, unchanged, on every request (llm.py:479-486); and `AgentRunner._run_loop` (llm.py:488-527) inspects `response.choices[0].message.tool_calls` itself, dispatches the named Python method directly against `self.tool_callbacks` (llm.py:363-368, still the four `_sumac_*` methods §18 specified), and appends the exchange as two new messages — `{"role": "assistant", "content": json.dumps({"name": call.name, "parameters": args})}` then `{"role": "tool", "content": result}`, matching `tool_call.py`'s exact shape — before sending again. Only the first tool call in a response is dispatched, matching the server-side loop's own documented one-call-per-round behavior (§7) and the system prompt's existing "one call per turn" instruction (§20, unchanged).

This changes what §12's `AgentPermission`/`session_id`/`max_tool_rounds` recommendations mean for the implemented code: none of the three are set on any `ChatCompletionRequest` `AgentRunner` builds (llm.py:479-486). `self._messages` — a plain Python list `AgentRunner` owns per `propose`/`revise` invocation (llm.py:361, 549-563) — replaces what §13 minted a `session_id` for; §10's open question about whether `remember_for_session` has a session to scope to without an explicit `session_id` no longer applies to the implemented design, since nothing in it uses `agent_permission` or `agent_approval_callback` at all. `MAX_TOOL_ROUNDS` (still 20, still §13's termination-guarantee reasoning) bounds `AgentRunner._run_loop`'s own `for` loop (llm.py:502) rather than a request field.

One addition beyond §16-§19's named types: `ToolCallRecord` (llm.py:299-308) and `AgentPlan.trace: tuple[ToolCallRecord, ...]` (llm.py:313-316), plus `render.print_trace` (render.py:289-303), called from `cli.py`'s `ask` loop before both the write-plan table and a read-only reply (cli.py:414). Motivated by testing against the real model above: a plain final reply like "the jam is in the fridge" gives no way to tell a `sumac_find_inventory` call that found nothing from one the model just summarized loosely, without seeing the tool's own raw JSON result. `AgentRunner.propose`/`.revise` reset `self._trace` at the start of each call and return it via `dataclasses.replace(plan, trace=tuple(self._trace))` (llm.py:549-563) — the trace on a given `AgentPlan` covers only the round(s) that produced it, including any self-review pass, not prior `propose`/`revise` calls in the same conversation.

§9's tool-schema content (§17), §20's system prompt text, and §7's factual findings about mistral.rs's two loop modes are unaffected by this section — only the request-construction mechanism changed. Read §9, §12, and §13 as describing a mechanism this entry tried and found non-functional against the installed 0.9.2 Python bindings, not as the shape `sumac/llm.py` implements.

## 26. A duplicate tool call, traced to a hard ceiling in the Python SDK's message field

A real run of `sumac ask "where is the butter?"` (`unsloth/Qwen3-0.6B-GGUF`, `enable_thinking=False`) called `sumac_find_inventory` twice in a row with byte-identical arguments (`{"query": "butter"}`) before answering — visible directly in §25's `AgentPlan.trace` addition, which is what surfaced this rather than a guess. The immediate hypothesis was the message shape §25 already flagged as forced rather than idiomatic: `examples/python/tool_call.py`'s `{"role": "assistant", "content": json.dumps({"name": ..., "parameters": ...})}` puts the completed call in `content` as a JSON blob rather than in an OpenAI-style `tool_calls` field, so the GGUF chat template's own `{%- if message.tool_calls %}` branch — which would render a native `<tool_call>...</tool_call>` block signalling "already done" — never fires for it.

Tried the fix that hypothesis suggests: an `assistant` message carrying a real `tool_calls: [{"id": ..., "type": "function", "function": {"name": ..., "arguments": ...}}]` list plus a `tool` message carrying `tool_call_id`, matching `tool-calling-basics.mdx`'s HTTP example. Traced against `mistralrs-pyo3/src/lib.rs`'s `send_chat_completion_request` (fetched fresh from `master`) before shipping it: the loop that converts each incoming Python message dict into the Rust request reads exactly two keys, `message["role"]` and `message["content"]`, and nothing else — any `tool_calls` or `tool_call_id` key on a dict passed through `ChatCompletionRequest.messages` is silently dropped before the request reaches the engine or the chat template. The `tool_calls`-carrying shape was reverted before landing in `sumac/llm.py`; `_run_loop` (llm.py:489-547) keeps the original content-blob shape, now with a comment recording why (llm.py:490-520) — it is the only channel available for round-tripping "a tool was already called and returned this" through the installed 0.9.2 Python bindings, not an arbitrary choice.

One change landed permanently from this investigation: `_print_usage` (llm.py, called from `_run_loop`) prints each round's `ChatCompletionResponse.usage` — `prompt_tokens`, `completion_tokens`, `avg_compl_tok_per_sec`, `total_time_sec`, all mistral.rs's own numbers, not sumac's — since nothing in the codebase surfaced token or timing counts before this, and a report of ~41s wall time for one `find`-only request had no other source of per-round data to diagnose against.

A second change was tried and reverted: adding "never call the same tool with the same arguments more than once" to `SYSTEM_PROMPT`. Run against a real model (`Qwen3-0.6B-GGUF`, same "where is the butter?" request), it did not fix the duplicate call — the model instead skipped calling `sumac_find_inventory` at all and answered "There is no butter inventory available," a wrong answer produced with zero tool calls. Reverted; not present in the committed `SYSTEM_PROMPT`. Recorded here because "prompt says do X" does not reliably produce X on a 0.6B model, and an untested prompt change is not evidence of anything — §27 finds a confirmed, separate bug in the same request instead of adding more prompt instructions on top of an unconfirmed guess.

## 27. `sumac_find_inventory` picked list position, not relevance — a confirmed bug, and a plausible but unconfirmed link to §26

`_print_usage`'s numbers, read against the real "where is the butter?" run (§26), show the prompt growing every round — 1184, then 1915, then 2646 tokens across three rounds, each round's growth accounted for almost entirely by the previous round's `sumac_find_inventory` tool result being appended to `self._messages` (§25's forced content-blob shape has no way to summarize or drop it). That result, for the query "butter" against this vault, has 14 matches: 2 literally named "Butter" (`fridge-door`, `hob-right-shelf-middle`), several with "butter" as a whole word ("Salted Butter", "Homemade Clarified Butter", two "Peanut Butter" products, "Waitrose All Butter Croissants", "Brussels Sprouts with Chestnuts and Honey Butter"), and several where "butter" is only a substring of a different word ("Butternut Box" dog food, six location entries). Before this section, `_sumac_find_inventory` (llm.py) returned these sorted by `(location_id, product_id)` — an arbitrary order with no relation to the query.

The run's final answer — "The butter is currently in the 'Big Freezer > Drawer 2' location" — names the location of "Brussels Sprouts with Chestnuts and Honey Butter," the first entry in that location-sorted list, not either of the two products actually named "Butter." This is a confirmed bug, not a hypothesis: a 0.6B model given an unranked list of near-duplicate JSON objects picked the first one rather than the one matching the query, and stated it as the unambiguous answer — precisely the failure §20's "ask when ambiguous" instruction was meant to prevent, which did not fire here because nothing in the tool result distinguished "this is the real answer" from "this superficially matches."

Fix: `_sumac_find_inventory` now sorts matches by `_find_match_rank` — 0 for an exact `product_id` match, 1 for the query appearing as a whole word (`\bquery\b`, catching "Salted Butter" and "Peanut Butter" but not "Butternut"), 2 for everything else — before falling back to alphabetical order within each tier. This is a narrow, deliberate divergence from `render.print_find`'s own ordering (location-then-product, no relevance ranking) — justified for the agent specifically because a human scanning `sumac find`'s full table has no "list position bias" problem, and a small model consuming one linear JSON blob does. `tests/test_llm.py` covers the reduced case (an exact "Butter" match must sort before a "Butternut Box" substring match) directly from this bug — later renamed `test_find_inventory_excludes_substring_only_match_by_default` once §29 changed the matching rule itself, not just the ordering.

Whether this fixes or reduces the duplicate call itself (§26) is not established — it addresses the confirmed ranking bug, and it also shrinks the "signal" the model has to extract correctly from the same-sized result, which is a plausible contributing factor to the model failing to recognize its own prior search as sufficient, but that causal link has not been tested against a real model as of this section. Not yet tried: reducing the token cost of the tool result itself (fewer fields per match, or a cap on how many matches are returned) — raised as a real, informative point but deliberately not acted on in this section, since it changes the tool's contract rather than just its ordering and deserves its own test rather than being bundled with the ranking fix.

## 28. Re-tested against a real model: §27 fixed the wrong answer, not the duplicate call — found the actual mechanism by reading the chat template line by line

Re-ran `sumac ask "where is the butter?"` against the real model after §27's ranking fix landed. The answer is now correct — "The butter is found in the fridge-door at Fridge > Door Shelves" — confirming §27's fix works as intended. The duplicate call is unchanged: `sumac_find_inventory {"query": "butter"}` still fires twice, back to back, with byte-identical results, before the model answers on the third round. §27's hypothesis that a cleaner result might incidentally fix this did not pan out.

Read the GGUF chat template dumped at every `sumac ask` startup character-by-character rather than skimming it, specifically the `{%- elif message.role == "assistant" %}` branch: a `<tool_call>{"name": ..., "arguments": ...}</tool_call>` block is only appended when `message.tool_calls` is truthy. §26 already established that `mistralrs-pyo3`'s message conversion drops any `tool_calls` field on an incoming Python dict — so an assistant message built through this SDK's `messages` list can *never* trigger that branch, no matter what's in the dict. What was missed in §26: that branch is the *only* place the template emits `<tool_call>` tags at all — every prior round's tool call, in this design, renders as plain assistant text with no tags. Separately, the template's own reverse pre-pass (computing `ns.last_query_index`, used to decide which assistant turns get `<think>` treatment) checks `message.role == "user"` — never `"assistant"` or `"tool"` — so nothing in the template's own logic ever specifically recognizes a prior assistant turn as "a tool call already happened" from content alone. The model has to infer that entirely from the surrounding text matching (or not matching) a pattern it was fine-tuned to recognize — and `{"name": "sumac_find_inventory", "parameters": {"query": "butter"}}` as bare assistant text, with the wrong key (`"parameters"` instead of the trained `"arguments"`) and no `<tool_call>` wrapper, does not match that pattern.

The fix does not require the dropped `tool_calls` field — `content` is passed through unchanged by the same binding code that drops `tool_calls`, and the template's tag-rendering logic is deterministic text concatenation the caller can reproduce by hand. `_run_loop` (llm.py) now sets the assistant message's `content` to the literal string `<tool_call>\n{"name": "<name>", "arguments": <raw arguments string>}\n</tool_call>` — using `message.tool_calls[0].function.arguments` verbatim (already a JSON string, exactly what the template inserts via `{{- tool_call.arguments }}` when `tool_call.arguments is string`) rather than re-serializing the parsed dict. Worked through by hand against the dumped template with `content=""` and one tool call: the resulting text is character-for-character what the native `message.tool_calls`-driven branch would have produced (`<|im_start|>assistant\n<tool_call>\n{"name": "X", "arguments": {...}}\n</tool_call><|im_end|>`), since the template's own rendering is just string concatenation the caller can reproduce without needing the dropped structured field at all. Not yet run against a real model as of this section — this is a specific, checked-by-hand mechanism, not a repeat of the untested prompt-line guess from §26.

## 29. Narrowing the match set itself, not just its order

§27 reordered `sumac_find_inventory`'s results but kept every substring match, including six "Butternut Box" dog-food entries for a "butter" query — user feedback on §27/§28's real run: "there is butter in the fridge, freezer, and a cupboard" (i.e. several genuine whole-word matches the person would count as an answer) "but that is more an issue that the find tool is primitive," suggesting whole-word matching as the default rather than a tie-break.

`_sumac_find_inventory` (llm.py) now computes `_find_match_rank` for every substring match as before, but filters to rank ≤ 1 (exact or whole-word) when any such match exists, falling back to the full substring-matched set only when nothing matches as a whole word — so a search that only makes sense as a substring (e.g. "nut", matching only "Butternut") still returns something rather than nothing. For the real "butter" query this drops from 14 matches to 9 (2 exact + 7 whole-word), removing the "Butternut Box" entries entirely rather than merely ranking them last. The tool's own schema description (§17) was updated to state this — whole-word by default, substring fallback — since that's now a fact about the tool's contract, not a new instruction to the model. `tests/test_llm.py` covers both the narrowing (`test_find_inventory_excludes_substring_only_match_by_default`) and the fallback (`test_find_inventory_falls_back_to_substring_when_no_whole_word_match`).

Deliberately not done in this section: reducing the *fields* returned per match (§27 raised this; still untried) — narrowing which matches are included is a different lever from shrinking each match's own payload, and this section only pulls the first one.

## 30. Two UX gaps, unrelated to the model behavior above

Two issues raised independently of the duplicate-call investigation:

**`--dry-run` was invisible for a read-only request.** `cli.py`'s `ask` loop returned identically whether `--dry-run` was passed or not whenever `plan.writes` was empty — there was no branch in that code path that read `dry_run` at all. For a request like "where is the butter?" (never produces a `ProposedWrite`), this meant `--dry-run` had zero observable effect, which reads exactly like "the flag doesn't do anything" even though the flag's actual job (never call `agent.commit()`, so `store.append` never runs) is unaffected — a write-producing request already never commits under `--dry-run`, because `cli.py`'s loop returns right after `render.print_plan(plan)` and before the accept/reject/feedback prompt that leads to `agent.commit(plan)`. The fix is cosmetic, not behavioral: `cli.py`'s `ask` loop now prints an explicit `(--dry-run: ...)` line in both the read-only and write-plan branches, so the flag's effect is visible even when there was nothing to suppress. Covered by `tests/test_cli.py`'s `test_ask_dry_run_on_read_only_request_still_shows_dry_run_indicator` and an added assertion in the existing write-plan dry-run test.

**`_print_usage`'s lines had no round number.** With three rounds producing three near-identical `"N prompt + M completion tokens, ..."` lines and a separate trace table with its own two rows, there was no way to tell which usage line corresponded to which trace row without counting. `_print_usage` (llm.py) now takes a `round_num` argument and prefixes each line with `round {round_num}:`; `_run_loop`'s own round counter (previously a bare `for _ in range(...)`, now `for round_num in range(1, MAX_TOOL_ROUNDS + 1)`) supplies it.

## 31. §28 confirmed against a real model; a second, distinct failure mode found on a different query

Re-ran `sumac ask "where is the butter?"` (both plain and `--dry-run`) against `Qwen3-0.6B-GGUF` after §28's `<tool_call>` fix landed. `sumac_find_inventory` now fires exactly once — round 1 calls it, round 2 answers correctly ("The butter is currently in the fridge-door location"). Wall time dropped from ~40s (§26's original report, two duplicate calls, unranked results) to ~7.7s (one call, §29's narrowed 9-match result instead of 14). §28's fix is confirmed, not just checked by hand.

A different query on the same model surfaced a different, unrelated failure: `sumac ask "where is the water?"` produced no tool call at all — round 1 (the only round) replied with the plain text `Ask in plain text without using any tool calls. "Where is the water?"`, closely paraphrasing `SYSTEM_PROMPT`'s own closing instruction ("respond in plain text with no further tool calls") rather than answering or searching. `sumac_find_inventory` would have returned real, unambiguous whole-word matches for "water" (`Bottled Water`, `Sparkling Water`, `Still Water`, and others) had it been called — this is not a case §29's narrowing failed to help; the model never attempted a search. The same request, same vault, same prompt, against `Qwen3-1.7B-GGUF` (no other change), searched correctly on the first round and produced a proper "multiple matches, please clarify" reply listing all seven results — the disambiguation behavior §20's system prompt asks for, working as intended at the larger size. This is a controlled comparison (one variable changed) pointing at model capacity, not at anything in `sumac/llm.py` — recorded here rather than chased further, since neither the tool-calling loop (§25/§28) nor the tool's own matching (§27/§29) failed on this query; the model simply didn't attempt a tool call.

`_print_usage`'s round labels (§30) turned out not to be enough on their own to diagnose the "water" failure — the round 1 usage line (`1226 prompt + 17 completion tokens, ...`) gave no hint of *what* those 17 completion tokens said without separately locating them in the raw log output. `_print_usage` now includes a same-line preview of what the round actually produced — `tool call: name(arguments)` for a dispatched call, or up to 100 characters of the reply text otherwise — via a new `_round_preview` helper, so the "water" failure's actual text is visible on the same line as its token count rather than requiring a second lookup.

The section below records repository state in the format specified by `docs/JOURNAL.md`. §1-§6 above predate that format's adoption for this entry and follow the `docs/journal/2026-08-31-*` entries' convention of a free-form design record followed by a compliant tail section.

---

# 2026-09-01: `sumac ask` Implementation State

This entry's format is scoped by `docs/JOURNAL.md` to repository state, superseding the "Scaffolding State" tail section this entry originally shipped with (before §16-§25's implementation work). §25 records why the implemented request-construction mechanism differs from §9/§12's recommendation; this section describes what the resulting code and tests actually do.

## Current State

- `src/sumac/llm.py` (594 lines) defines `AgentRunner`, `AgentPlan`, `ProposedWrite`, `ToolCallRecord`, and `SendsCompletions` (llm.py, full file).
- `AgentRunner.__init__(data_dir, key, *, runner=None)` builds a real `mistralrs.Runner` from `Which.GGUF(quantized_model_id="unsloth/Qwen3-0.6B-GGUF", quantized_filename="Qwen3-0.6B-Q4_K_M.gguf")` with no `tool_callbacks` argument, or accepts an injected `runner` satisfying `SendsCompletions` (llm.py:95-96, llm.py:329-346, llm.py:356-378) — changed from `Qwen3-1.7B-GGUF` after the two real runs recorded near the end of this section.
- `AgentRunner._build_request` sets `enable_thinking=False` on every `ChatCompletionRequest` (llm.py:479-487), disabling Qwen3's default `<think>...</think>` reasoning block for every `sumac ask` call.
- `_print_usage(response, round_num)` prints each round's `ChatCompletionResponse.usage` prefixed with `round {round_num}:` (§30) and suffixed with a same-line preview from `_round_preview` — `tool call: name(args)` or up to 100 characters of the reply text (§31) — prompt/completion token counts and mistral.rs's own `avg_compl_tok_per_sec`/`total_time_sec` in between; called from inside `AgentRunner._run_loop` after every `send_chat_completion_request`, a no-op when `response.usage` is absent, which is the case for every hand-built fake in `tests/test_llm.py` and `tests/test_cli.py`.
- `AgentRunner.propose(prompt)` and `.revise(feedback)` drive a client-side tool-calling loop against `self._messages`, a plain Python list the instance owns — `_run_loop` dispatches `response.choices[0].message.tool_calls[0]` against `self.tool_callbacks` directly and appends the exchange as new messages, rather than registering `tool_callbacks` on `Runner` (see §25 for why). The assistant message for a dispatched call is the literal string `<tool_call>\n{"name": ..., "arguments": ...}\n</tool_call>` (§28), not a `{"name", "parameters"}` JSON blob.
- `AgentRunner.tool_callbacks` holds four entries, each named with a `sumac_` prefix — `sumac_find_inventory`, `sumac_consume_inventory`, `sumac_move_inventory`, `sumac_discover_inventory` — matching §17's schemas with names updated (llm.py:112-267, llm.py:363-368).
- `sumac_find_inventory`'s callback matches `ledger.build_inventory` on `product_id`, case-insensitive, defaulting to whole-word matches only and falling back to any substring match when nothing matches as a whole word (§29) — a deliberate divergence from `render.print_find`'s plain substring rule. Results are sorted by `_find_match_rank` (exact, then whole-word, then substring) before alphabetical (§27), and `location_path` comes from `config.location_path` (llm.py, `_sumac_find_inventory`, `_find_match_rank`).
- `sumac_consume_inventory`/`sumac_move_inventory`/`sumac_discover_inventory` all call `AgentRunner._propose_write`, which calls `decide.decide_change` and appends a `ProposedWrite` to `self._pending` on success, or returns a `{"status": "rejected", "reason": ..., "detail": ...}` JSON string on a caught `Rejected` (llm.py:400-466).
- `AgentRunner.commit(plan)` re-calls `decide.decide_change` for each `ProposedWrite` against freshly reloaded `ledger.build_inventory`/`config.build_config`, prints any `decide_change` messages via `render.print_warning`, then `store.append`s the resulting writes (llm.py:566-594).
- `AgentPlan.trace: tuple[ToolCallRecord, ...]` (llm.py:299-316) records every tool call `_run_loop` dispatched during one `propose`/`revise` call, including any self-review pass; `propose`/`revise` reset `self._trace` at the start of each call and attach it to the returned `AgentPlan` via `dataclasses.replace` (llm.py:549-563), so a given plan's trace does not include an earlier `propose`/`revise` call in the same conversation.
- `src/sumac/cli.py`'s `ask` command takes `prompt: str`, `--dry-run`, and `--data-dir` (cli.py:373-380); its loop calls `render.print_trace(plan.trace)` on every iteration before either `render.print_plan(plan)` (writes present) or printing `plan.reply_text` (no writes), and prompts for `accept`/`reject`/free-text feedback unless `--dry-run` is set, in which case it prints an explicit `(--dry-run: ...)` line before returning either way (§30).
- `cli.py`'s `ask` command lets a `Rejected` raised inside `agent.commit(plan)` propagate uncaught to `cli.main`'s top-level handler; the surrounding `try/except Exception` covers only `AgentRunner(...)` construction and `.propose()`/`.revise()` (cli.py:403-411, cli.py:436-440).
- `src/sumac/render.py` defines `print_trace(trace)` and `print_plan(plan)`, both importing `sumac.llm`'s types only under `TYPE_CHECKING` to avoid a circular import (`llm.py` imports `render` at module level for `print_warning`/`console.print`) (render.py:6, render.py:14-15, render.py:289-321).
- `tests/test_llm.py` drives `AgentRunner` against a hand-built `FakeRunner` satisfying `SendsCompletions`; one `ScriptedResponse` is one `send_chat_completion_request` round (a single tool call or a final reply), not a whole multi-call turn, matching the client-side loop's actual call granularity (tests/test_llm.py, full file).
- `tests/test_cli.py` has eight `test_ask_*` functions (read-only reply, tool-call trace display, `--dry-run` on a write-plan and on a read-only reply, accept, reject, feedback-then-accept) driving the `ask` command's interactive loop against a scripted `_FakeAgentRunner` monkeypatched onto `sumac.llm.AgentRunner`.
- `pytest` reports 226 passing tests repo-wide; `ruff format --check .`, `ruff check .`, and `ty check` each report no findings (commands run from the repository root).
- `sumac ask "where is the jam?" --dry-run`, run against a real vault and `Qwen3-1.7B-GGUF` on the user's machine, completes without the tool-registration error §25 records and asks a disambiguating question naming two "jam"-matching products in the vault (Scotch Bonnet Jam, Jamaican Style Hot Sauce) rather than guessing between them.
- `sumac ask "where is the strawberry jam?" --dry-run`, run the same way immediately after, reports no match for "strawberry jam" rather than crashing or hanging.
- `sumac ask "where is the butter?"` against `Qwen3-0.6B-GGUF` with `enable_thinking=False`, run across §27/§28/§29's fixes in sequence: before §27, names an unrelated dish's location as the answer; after §27 but before §28, correct location but still two identical `sumac_find_inventory` calls; after §28 and §29 (§31), exactly one call and the correct location, in ~7.7s wall clock including model load (down from the original ~40s with two duplicate calls and an unranked 14-match result).
- `sumac ask "where is the water?"` against `Qwen3-0.6B-GGUF` produces no tool call at all — a plain-text reply paraphrasing `SYSTEM_PROMPT`'s own closing instruction rather than searching or answering (§31). The identical request against `Qwen3-1.7B-GGUF`, no other variable changed, searches correctly on the first round and asks the person to clarify among the resulting real matches, as §20's system prompt asks for.

## Stubbed

- None found in `ask`'s own code path as of this entry — the two `ask`-specific issues the prior "Scaffolding State" tail section recorded under Stubbed (an `except ImportError` branch that misattributes a `sumac.llm`-side failure to a missing `mistralrs`, and a `commit`-time `Rejected` caught by the same handler as a genuine bug) are both addressed in Current State above: `sumac.llm` exists, and `commit` is outside the `try/except Exception` block (cli.py:403-411, cli.py:430).

## Missing

- No automated test exercises `AgentRunner` against a real `mistralrs.Runner` — `tests/test_llm.py`'s `FakeRunner` and `tests/test_cli.py`'s `_FakeAgentRunner` are both hand-built stand-ins (§22's intended split); the manual runs recorded under Current State are the only real-model exercise of this code, and none is captured as a test.
- No compound multi-write request (the ragu/tomatoes reference example, §2) or any write-producing request (`consume`/`move`/`discover`) has been run against a real model — every manual run recorded above is read-only (`find` only).
- §31 found a second, unrelated real-model failure mode: on some queries (confirmed: "where is the water?"), `Qwen3-0.6B-GGUF` skips calling any tool at all and instead replies with text paraphrasing `SYSTEM_PROMPT`'s own instructions — not explained or fixed by §25/§27/§28/§29, and reproducible as a controlled A/B against `Qwen3-1.7B-GGUF` (identical request, same vault, only the model changed) that does not show the failure.
- §27/§29 explicitly did not act on reducing the tool result's token cost per match (fewer fields, e.g. dropping `location_id` since `location_path` already subsumes it) — raised as a real, informative point in the investigation but left as a separate, untried change.
- `QUANTIZED_MODEL_ID`/`QUANTIZED_FILENAME` (llm.py:95-96) — §31's "water" finding is the first controlled comparison between the two models this entry has, and it favors `Qwen3-1.7B-GGUF` on reliability where `Qwen3-0.6B-GGUF` is faster; no decision between them is recorded here.
- No flag or setting suppresses `render.print_trace`'s or `_print_usage`'s output — both print unconditionally whenever there is something to print.
- `README.md` contains no reference to `ask`, `agent`, or `mistral` in any case (README.md, full file, unchanged since the previous entry).

## Divergence

- None found against `README.md`, which makes no claims about `ask` to diverge from (see Missing, above).
